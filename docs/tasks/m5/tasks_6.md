# Goal: [M5-6] 운영 런북·2계정 E2E·문서 정합화

> **2026-09-23 범위 변경**: LLM 가사 정규화와 가사 라이브러리(카탈로그·가사 기여·대표 가사·곡 식별)는 MVP에서 제거됐다 (`packages/db/drizzle/0005_remove_catalog.sql`). 공유 라이브러리는 같은 곡을 여러 사람이 따로 공개하는 게시판(가져간 횟수순)만 남는다. 이 문서의 해당 부분은 이력으로 남긴다.

> **마일스톤**: M5 (공유·가사 라이브러리)
> **태스크 번호**: `tasks_6.md`
> **선행 조건**: `docs/tasks/m5/tasks_5.md` 완료 (공유 UI)
> **목표**: M5 완료 기준 두 가지를 테스트와 실제 브라우저로 고정하고, 관리자 화면 없이 운영자가 신고·게시 중단·대표 가사 교정을 할 수 있는 절차를 테스트로 보장한다. PRD·TECH_SPEC·AGENTS를 코드에 맞춘다
> **완료 기준 (DoD)**: (a) 다른 계정으로 공개 덱을 검색해 가져온 뒤 수정 없이 송출한다 — 브라우저에서 확인. (b) 같은 곡을 2개 계정이 등록하면 정규화 결과가 검증(4.8)을 통과한다 — worker E2E에서 확인(로컬 브라우저는 폴백 경로 확인)

> **구현 현황 (2026-09-23)**
>
> - Task 6.1~6.6 완료. 전체 **885개 / 106파일 Green**, `pnpm --filter web build` 통과.
> - **M5 완료 기준 (a)를 브라우저에서 확인했다** (§4). A가 곡을 공개하고, B가 검색해 가져와 세트에 담아 단독 전체화면으로 송출했다. 슬라이드 3장과 스타일(노란 글자·강한 그림자)이 A의 원본과 D1에서 글자 그대로 같다.
> - **완료 기준 (b)는 두 경로로 확인했다.** worker E2E(`sharing.e2e.test.ts`)는 가짜 모델로 `canonical_source='llm'` 결과가 `verifyNormalization`을 통과하는 것과 환각 시 폴백을 본다. 로컬 브라우저에서는 AI 바인딩이 원격 전용이라 호출이 실패하고(`Binding AI needs to be run remotely`) 최다 등록 버전으로 떨어졌다 — 설계대로의 폴백이다. **실제 Qwen 결과가 검증을 통과하는지는 런북 §6의 원격 확인으로 남는다.**
> - 브라우저 검증에서 고친 것 1건: 공개 대화상자의 조사 오류("'은혜로다'이").
> - **운영 D1 적용은 운영자 몫이다.** `pnpm --filter @repo/db db:migrate:prod`로 `0003_m5_sharing`·`0004_m5_fts`를 적용한다. `0004`는 공개 동의 기록이 없는 공개 덱을 모두 비공개로 되돌린다(지금까지 공개 경로가 없었으므로 샘플에서 흘러든 값뿐이다).

---

## 1. 아키텍처 가드레일 & 준수 사항

1. **운영 SQL은 한 곳에서 정의하고 테스트한다**: `packages/db/src/ops/moderationSql.ts`가 정본이다. 테스트가 실제 마이그레이션 스키마에 대해 실행하고, 런북에 글자 그대로 실려 있는지 확인한다.
2. **관찰 가능한 사실만 완료로 적는다**: 로컬에서 확인할 수 없는 것(실제 Qwen, 운영 D1)은 남은 일로 적는다 (docs/tasks/AGENTS.md §2).
3. **E2E는 실제 라우트로**: `createApp({ readSession, modelRunner })`로 프로덕션 라우트를 그대로 지나간다.

---

## 2. 세부 작업 체크리스트

- [x] **Task 6.1: 운영 SQL 정본 (TDD)**
  - **대상 파일**: `packages/db/src/ops/moderationSql.ts`, `moderationSql.test.ts`
  - **선행 조건**: `tasks_5.md` 완료
  - **구현 내용**: 신고 목록·처리·반려, 게시 중단·해제, 다시 공개된 사본 찾기, 대표 가사 잠금·해제, 곡 분리(새 카탈로그·버전 이동·버전 수 재계산), 카탈로그 삭제. `:name` 자리표시자. better-sqlite3로 실제 스키마에 실행
  - **DoD (통과 기준)**: `pnpm vitest run packages/db/src/ops/moderationSql.test.ts`가 100% 통과(Green)한다.

- [x] **Task 6.2: 운영 런북**
  - **대상 파일**: `docs/ops/moderation-runbook.md`, `packages/db/src/ops/runbook.test.ts`
  - **선행 조건**: Task 6.1
  - **구현 내용**: `wrangler d1 execute` 실행법(자리표시자 치환, 여러 줄 가사는 `--file`), 신고 확인, 게시 중단, 대표 가사 교정·잠금, 곡 분리, 카탈로그 삭제, Qwen 원격 확인, AI Gateway 비용 상한. 모든 SQL이 정본과 글자 그대로 같은지 테스트
  - **DoD (통과 기준)**: `pnpm vitest run packages/db/src/ops/runbook.test.ts`가 100% 통과(Green)한다.

- [x] **Task 6.3: 2계정 E2E**
  - **대상 파일**: `apps/web/worker/e2e/sharing.e2e.test.ts`
  - **선행 조건**: Task 6.1
  - **구현 내용**:
    - (a) A 공개 → B 검색(미리보기만) → 상세 → 가져오기 → 세트 복제본 저장 → B가 받은 세트의 슬라이드·스타일이 원본과 같고 비공개, A의 가져간 횟수 1
    - (b) A·B가 같은 곡을 띄어쓰기만 다르게 등록 → 백그라운드 정규화 → `normalized`/`llm`, `verifyNormalization` 통과, 검색에 2명 등록
    - (b') 모델이 없는 줄을 지어내면 `popular_root`
  - **DoD (통과 기준)**: `pnpm vitest run apps/web/worker/e2e/`가 100% 통과(Green)한다.

- [x] **Task 6.4: 브라우저 실검증 (dev login 2계정)**
  - **대상 파일**: 이 문서 §4
  - **선행 조건**: Task 6.3
  - **구현 내용**: 로컬 D1에 마이그레이션 적용 → `pnpm --filter web dev` → A 등록·공개 → B 검색·가져오기·송출 → B가 같은 곡을 다른 아티스트 표기로 등록('이 곡이 맞나요?') → 배지 확인 → IndexedDB 삭제 후 보관함 복원
  - **DoD (통과 기준)**: §4 표의 모든 항목이 기대 결과와 같다.

- [x] **Task 6.5: 문서 정합화**
  - **대상 파일**: `docs/TECH_SPEC.md`(§2.2·§4.2·§4.3·§6.1·§7.1·§8.1·§9), `prd.md`(§5 화면 표, §8 현황·M5 행), `docs/tasks/AGENTS.md`(§2.1, §6 표)
  - **선행 조건**: Task 6.4
  - **구현 내용**: 실제 엔드포인트·마이그레이션·FTS 조건·정규화 폴백·운영 절차를 반영한다. 편차(교정 제안 수동 접수, 운영자 SQL 런북)를 적는다. `docs/tasks/AGENTS.md` §6 표 밖으로 떨어져 있던 '가사 정규화' 행을 표 안으로 옮긴다
  - **DoD (통과 기준)**: `grep -n "미구현" docs/TECH_SPEC.md`에 M5 항목이 없다 (남은 것은 커스텀 배경 업로드뿐).

- [x] **Task 6.6: 전체 검증**
  - **대상 파일**: 없음
  - **선행 조건**: Task 6.1~6.5
  - **DoD (통과 기준)**: `pnpm typecheck && pnpm lint && pnpm test && pnpm --filter web build`가 모두 통과한다.

---

## 3. 검증 명령어

```bash
pnpm vitest run packages/db/src/ops apps/web/worker/e2e
pnpm typecheck && pnpm lint && pnpm test && pnpm --filter web build
grep -rn "COMMUNITY_SONGS" apps/web/src   # 0건
```

---

## 4. 브라우저 실검증 기록 (2026-09-23, Claude 브라우저 패널 + 로컬 dev 서버)

준비: `pnpm --filter @repo/db db:migrate:local`로 `0003`·`0004` 적용 → `pnpm --filter web dev` (`DEV_LOGIN_ENABLED`). 계정은 개발자 로그인 `a@worship.local`, `b@worship.local`.

| 단계                                                             | 기대 결과                                                                                                                                                     | 결과             |
| ---------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
| A 곡 추가 모달 첫 화면                                           | 샘플 공유 곡 없음, '내 곡 0 · 공유 0', 오프라인 안내 없음                                                                                                     | 일치             |
| A 직접 등록 '은혜로다 / 예수전도단' (기여 켬)                    | 세트에 추가, D1 `decks` 보관함 행 `origin=user, contribute_to_catalog=1`, 카탈로그 연결, `lyrics_versions` 1건, 세트 복제본은 `forked_from`=보관함 덱·비공개  | 일치             |
| A 스타일 변경(강한 그림자·노란 글자) 후 '공유 라이브러리에 공개' | 저작권 안내·보관함 덮어쓰기 안내, 동의 전 '공개하기' 비활성 → 동의 후 '공개 중 · 0회 가져감'                                                                  | 일치             |
| B 곡 추가 모달                                                   | 공유 곡 '은혜로다'(작성자 a, 0회 가져감)와 가사 라이브러리 '은혜로다'(1명 등록). 공유 곡은 로그인 상태라 전문 7줄 미리보기                                    | 일치             |
| B '가져와서 프레젠테이션에 추가'                                 | 세트에 3장, 속성 패널 '비공개 — 나만 볼 수 있습니다', '원작: a', '원본에 교정 제안'                                                                           | 일치             |
| D1 비교                                                          | A 원본·B 포크·B 세트 복제본의 슬라이드와 `fontColor=#FEF08A`, `textShadowLevel=strong`이 같다. A `fork_count=1`, B 덱은 비공개                                | 일치             |
| B 예배 준비 → 단독 전체화면 송출, → 키                           | A의 가사가 노란 글자로 송출되고 다음 슬라이드로 넘어간다 (배경은 로컬 R2에 영상이 없어 검정 — 환경 문제)                                                      | 일치             |
| 송출 중 네트워크                                                 | 앱 API 호출 없음. 보이는 요청은 `<video src>`의 배경 영상·포스터(로컬 R2 404)뿐                                                                               | 일치             |
| B 직접 등록 '은혜로다 / YWAM'(띄어쓰기 다름, 기여 켬)            | '이 곡이 맞나요?'에 기존 곡 후보 → 선택 후 저장                                                                                                               | 일치             |
| 카탈로그                                                         | 한 곡에 버전 2, 백그라운드 정규화 실행, 로컬은 AI 원격 전용이라 `popular_root`(로그: `model error: Binding AI needs to be run remotely`), `status=normalized` | 일치 (폴백 경로) |
| B 곡 추가 모달 재진입                                            | '정규화됨 · 2명 등록' 배지, 가져온 공유 곡은 '보관함에 있음'                                                                                                  | 일치             |
| B 로컬 IndexedDB 삭제 후 재접속                                  | 보관함 곡 2건(포크·직접 등록)이 서버에서 복원 (M5-2 완료 기준)                                                                                                | 일치             |

### 4.1 확인하지 못한 것

- 실제 Qwen3.8 27B 호출 결과 — 런북 §6 원격 확인 절차
- 실제 카카오·네이버 계정 2개(M3-B와 같은 대기 사항). 이 검증은 개발자 로그인으로 했다
- 오프라인 곡 추가 모달은 테스트(`SongPickerModal.test.tsx` '오프라인')로만 확인했다. 브라우저 패널에서 네트워크를 끊지 않았다

---

## 5. 이 마일스톤 이후

M6(가사 시드) — 운영자가 CCM 100곡을 입력·공개하고 런북 §3의 `LOCK_CATALOG`로 대표 가사를 잠근다. 개발 작업은 없다.
